/**
 * @pos/api — Cloudflare Workers 伺服端。
 *
 * 排定於重構規劃書 P2（伺服端）階段實作：Hono + @hono/zod-openapi 路由、
 * Drizzle ORM 對接 D1、以 @pos/contract 的 schema 驗證請求並重算金額。
 *
 * P0 階段本檔案僅作為 workspace 套件邊界的佔位，不含任何伺服端邏輯。
 */
export {}
