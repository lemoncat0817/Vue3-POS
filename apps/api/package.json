{
  "name": "@pos/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.worker.json && tsc --noEmit -p tsconfig.test.json",
    "test": "vitest run",
    "dev": "wrangler dev",
    "db:generate": "drizzle-kit generate",
    "db:migrate:local": "wrangler d1 migrations apply pos-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply pos-db --remote",
    "db:seed:local": "wrangler d1 execute pos-db --local --file=./seed/catalog.sql && wrangler d1 execute pos-db --local --file=./seed/staff.sql && wrangler d1 execute pos-db --local --file=./seed/promotions.sql && wrangler d1 execute pos-db --local --file=./seed/payment-methods.sql && wrangler d1 execute pos-db --local --file=./seed/invoice-tracks.sql",
    "db:seed:remote": "wrangler d1 execute pos-db --remote --file=./seed/catalog.sql && wrangler d1 execute pos-db --remote --file=./seed/staff.sql && wrangler d1 execute pos-db --remote --file=./seed/promotions.sql && wrangler d1 execute pos-db --remote --file=./seed/payment-methods.sql && wrangler d1 execute pos-db --remote --file=./seed/invoice-tracks.sql",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@hono/zod-openapi": "^1.6.3",
    "@pos/contract": "workspace:*",
    "@pos/domain": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.13.7",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260907.1",
    "@types/better-sqlite3": "^9.6.0",
    "better-sqlite3": "^13.0.3",
    "drizzle-kit": "^0.31.10",
    "vitest": "^3.2.7",
    "wrangler": "^4.129.0"
  }
}
